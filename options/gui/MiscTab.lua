
local addon = Overseer
local options = addon:GetModule(addon.OPTIONS_MODULE)
local AG = LibStub("AceGUI-3.0")

local consts = options.consts

-- ------------------------------------------------------------------
-- 'Misc' tab
-- ------------------------------------------------------------------
options.tab[consts.tabs.MISC] = function(container)
end
